import { MngtypeDirective } from './mngtype.directive';

describe('MngtypeDirective', () => {
  it('should create an instance', () => {
    const directive = new MngtypeDirective();
    expect(directive).toBeTruthy();
  });
});
