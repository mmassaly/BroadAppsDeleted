import { MngvalueDirective } from './mngvalue.directive';

describe('MngvalueDirective', () => {
  it('should create an instance', () => {
    const directive = new MngvalueDirective();
    expect(directive).toBeTruthy();
  });
});
